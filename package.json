{
  "name": "llave-fullstack",
  "private": true,
  "version": "0.3.0",
  "scripts": {
    "install-all": "npm --prefix server install && npm --prefix web install",
    "build": "npm --prefix web install && npm --prefix web run build && npm --prefix server install",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
